<%= render :partial => "layouts/admin_header" %>
<h1>Listing panels</h1>

<table class="table">
  <tr>
    <th>Name</th>
    <!--<th>Title</th>-->
    <!--<th>Category</th>-->
    <!--<th>Subcategory</th>-->
    <th>Color</th>
    <!--<th>Body</th>-->
    <!--<th>Description</th>-->
    <!--<th>Featured</th>-->
    <th>Active</th>
    <th></th>
    <th></th>
  </tr>

<% @panels.each do |panel| %>
  <tr>
    <td><%= panel.name %></td>
    <!--<td><%= panel.title %></td>-->
    <!--<td><%= panel.category %></td>-->
    <!--<td><%= panel.subcategory %></td>-->
    <td><%= panel.color %></td>
    <!--<td><%= panel.body %></td>-->
    <!--<td><%= panel.description.blank? ? "" : panel.description.truncate_html(100, at_end="&hellip;").html_safe %></td>-->
    <!--<td><%= panel.featured %></td>-->
    <td><%= panel.active %></td>
    <td><%= link_to 'Edit', edit_panel_path(panel) %></td>
    <td><%= link_to 'Destroy', panel, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Panel', new_panel_path %>
